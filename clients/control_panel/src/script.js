const BPM_CONTAINER = $('#bpm-widget');
const SYNTH_CONTAINER = $('#synth-widget');
const BEAT_CONTAINER = $('#beat-widget');
makeBpmWidget(BPM_CONTAINER);
makeSynthWidget(SYNTH_CONTAINER);
makeBeatWidget(BEAT_CONTAINER);
